{
  "name": "@am-crm/db",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "migrate": "prisma migrate dev --name init",
    "generate": "prisma generate",
    "test": "bun test"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0"
  }
}
