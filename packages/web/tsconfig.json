{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "jsx": "preserve",
    "outDir": "dist",
    "strict": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": ["app", "testing", ".next/types/**/*.ts"]
}
