{
  "name": "@am-crm/telegram-bot",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outdir dist",
    "test": "vitest"
  },
  "dependencies": {
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
